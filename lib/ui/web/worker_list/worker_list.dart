import 'package:flutter/material.dart';

class ShiftsList extends StatelessWidget {
  const ShiftsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Список Работников'),
      ),
    );
  }
}
